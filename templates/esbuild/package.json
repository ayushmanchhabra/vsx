{
    "name": "vsx-esbuild-template",
    "private": true,
    "devDependencies": {
        "vsx": "*"
    },
    "scripts": {
        "start": "esbuild ./main.tsx --bundle --platform=node --outdir=. --watch --servedir=."
    }
}
